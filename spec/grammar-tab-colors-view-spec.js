'use babel';

import GrammarTabColorsView from '../lib/grammar-tab-colors-view';

describe('GrammarTabColorsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
